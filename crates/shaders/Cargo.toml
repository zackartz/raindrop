[package]
name = "shaders"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["dylib"]

[dependencies]
spirv-std.workspace = true
glam.workspace = true

shaders-shared = { path = "../shaders-shared" }
